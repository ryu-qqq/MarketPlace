# ============================================================
# SQS Consumer 프로덕션 환경 설정
# ============================================================
spring:
  cloud:
    aws:
      region:
        static: ap-northeast-2
      sqs:
        enabled: true
