# ============================================================
# SQS Consumer 로컬 개발 환경 설정
# ============================================================
# LocalStack 사용
# ============================================================
spring:
  cloud:
    aws:
      endpoint: ${SQS_ENDPOINT:http://localhost:4566}
      region:
        static: ap-northeast-2
      sqs:
        enabled: true
