# ============================================================
# FileFlow SDK 공통 설정
# ============================================================
# 환경변수로 제어:
#   - FILEFLOW_BASE_URL: FileFlow 서버 URL (필수)
# ============================================================

fileflow:
  base-url: ${FILEFLOW_BASE_URL:}
  timeout:
    connect: 5s
    read: 60s
